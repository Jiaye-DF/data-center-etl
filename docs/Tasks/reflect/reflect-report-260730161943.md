# reflect-report-260730161943

> 觸發:version-end(v1.5.2 + v1.6.0 收口)|素材:全版本 fixed.md(v1.0.0×3、v1.1.0×20、v1.4.0×5、v1.4.1×6、v1.5.0×6、v1.5.1×4;**v1.5.2 / v1.6.0 均無 fixed.md 條目,零新增**)|歷史報告:260706/260709/260710/260720/260721(累計 19 條候選全數待決議)
> 補充素材(裁定型實證,依 260720 候選 1 / 260721 候選 3 慣例):`propose-v1.5.2.md § 決策記錄`、`propose-v1.6.0.md § 決策記錄 + 變更紀錄`(task-009/010 兩次裁定推翻)、`task-004/005/007` 記錄在案之 4 項 reflect 候選、`Issue-Scan-Project-260730160810.md`(AD-133~150)

## 候選 1 — `03-passwords-and-pii` 不可逆雜湊原則:機器憑證(API client secret)雙軌例外成文化(規範被推翻)

- **類型**:修正
- **來源**:`propose-v1.6.0.md § 變更紀錄 2026-07-30`(task-009 落地):user 明示裁定「client_secret 改可逆加密儲存 + 儀表板明文檢視」,**推翻 `docs/Design-Base/04-databases/03-passwords-and-pii.md`「不可逆雜湊」原則之適用範圍**(理由:僅 admin 可及)。無 fixed.md 條目可鏈(v1.6.0 無 fixed.md),屬裁定型實證。
- **pattern**:規範被推翻判準成立 — 且推翻的不是個案寫法而是原則的**適用範圍**:規範原文以「密碼」為對象,未區分「人類密碼」與「機器對機器憑證」;後者的業務現實是 admin 需重新取回明文交付對接方(單鑰制下丟失即輪替固然可行,但 user 裁定要可檢視)。若不成文,下一個機器憑證場景(未來 webhook secret、外部系統 API key 託管)會重演同一輪推翻。
- **建議**:`04-databases/03-passwords-and-pii.md` 補一段〈機器憑證例外〉:(1) 人類使用者密碼一律不可逆雜湊,無例外;(2) 機器對機器憑證若業務需明文檢視,允許**雙軌制** — 驗證路徑仍走不可逆雜湊(bcrypt/argon2,零變動)、檢視路徑另欄存強加密(AEAD,金鑰 env 注入 + fail-fast、禁入版控),檢視端點限 admin + 每次檢視寫 audit(detail 禁明文);(3) 註明先例 v1.6.0 task-009(`api_client_secrets.secret_encrypted`,Fernet)。
- **影響**:既有 code 即 task-009 實作,天然合規,無 grandfather 負擔;不破壞 backward;需同步 — `docs/Prompts/scan-project.md § R-DB-003` 註記例外樣式(避免未來掃描誤報),`99-code-review/04-lint-checklist.md` 若有密碼段補「雙軌例外須四要件齊備(雜湊驗證/強加密/admin-only/audit)」。順帶收口:scan `AD-137`(Fernet key 只驗長度)修正後,「金鑰 env fail-fast」要件才算真正落實。
- **driver**:user(裁定人)+ 後端 reviewer

## 候選 2 — 內省 / 自動發布的範圍圈定:禁「全庫預設」,必須顯式白名單(觀察名單升格)

- **類型**:新增
- **來源**:fixed.md `v1.5.1 §1`(在目標 RDS 建 `erp_metadata` 實體表後,快照內省把系統 schema 當業務分類收進 — 260721 觀察名單第 2 條之原始事證);佐證:scan `AD-145`(v1.5.2 autofill 內省目標 RDS 全庫,任何落地的表 — 暫存表、實驗表 — 下輪即被自動補 confirmed 映射並生成對外 view;自家測試 `test_seed_semantic_mappings.py:85-92` 的 AF_TEST/VG_TEST 遺留 schema 污染即現成實證)、scan `AD-129`(v1.5.1:`_view`/`_en` 後綴排除靜默作用於 source 側,同屬「範圍界定靠隱式慣例」家族)
- **pattern**:260721 觀察名單第 2 條(單版本單條暫不成案)於 v1.5.2 **再現且後果升級**:同類根因(內省 / 自動化流程的作用範圍靠「全庫掃描 + 排除清單」的隱式界定,而非顯式圈定)跨 2 個版本累積 3 條事證,且後果從「檢視面污染」(v1.5.1)升級為「**自動發布對外**」(v1.5.2 autofill 直落 confirmed → view → JSON)。找不到對應規則 → 判準二成立。
- **建議**:`04-databases/01-identifiers.md`(或 `04-databases/00-overview.md` 若無更合適段)新增〈內省與自動化範圍圈定〉:(1) 任何「內省 DB 結構後自動採取行動」(自動建映射、自動生 view、自動發布)的流程,作用範圍必須**顯式圈定**(傳入本輪處理的表清單 / 白名單),禁以「全庫掃描 - 排除清單」為預設;(2) 純檢視類內省(瀏覽、統計)可全庫,但系統自建物件(metadata 表、`_view`/`_en` schema)須登記於**集中的系統保留命名清單**,各內省點引用同一清單排除;(3) 自動化流程須將本輪實際作用的物件清單寫入 log / run 紀錄供人審。
- **影響**:既有 code **不合規兩處**:v1.5.2 autofill(scan AD-145,修正 = `worker/tasks.py` 掛接處傳入本輪 targets 清單,小改)與排除清單散落(`introspect.py` / `semantic_autofill.py` 各自維護)— 建議隨規則採納開一個小補洞 task 一起收;不破壞 backward(新規則規範該 commit 之後,既有兩處以補洞 task 處理非追溯究責);需同步 `docs/Prompts/scan-project.md § 心法`巡視面向補「自動化流程作用範圍」一條。
- **driver**:user + ETL/後端 reviewer

## 候選 3 — `07-rule-evolution` 素材定義過窄:propose 決策記錄應成為 reflect 正式素材(流程與實務脫節)

- **類型**:修正
- **來源**:`07-rule-evolution.md § B 段`明定素材僅 `docs/Tasks/v*/fixed.md`;但實務上規範被推翻的裁定已連三輪改記於 propose 決策記錄而非 fixed.md — 260720 候選 1(事後追認型,首開慣例)、260721 候選 3(來源 = `propose-v1.5.1.md § 決策記錄`,明註「依慣例列案」)、本報告候選 1(來源 = `propose-v1.6.0.md § 變更紀錄`)。且 v1.5.2 / v1.6.0 兩版 fixed.md **零條目**(multi-agent 流程下違規多在 commit 前修畢,不落 fixed.md),若嚴格照素材定義,本輪 reflect 將無米可炊,而兩版實際發生了兩次裁定推翻 + 4 項記錄在案候選。
- **pattern**:同類根因(素材定義與實際紀錄慣例脫節)跨 ≥3 份報告持續發生,每次都靠「依前例」的口頭慣例撐 — 慣例本身從未成文,新 session / 新 agent 跑 reflect 時無據可依,判準三(規範與實務矛盾)成立。
- **建議**:`01-propose/07-rule-evolution.md § B 段`素材清單由「`docs/Tasks/v*/fixed.md`(全版本)」擴為三源:(1) fixed.md(全版本,主素材);(2) **propose 決策記錄 / 變更紀錄中標記「推翻」「裁定」的條目**(裁定型實證,正式化 260720 起之慣例);(3) task 檔中明文「列升規候選 / reflect 可討論」的記錄。同步 `docs/Prompts/reflect-rules.md § 前置讀取` 與 `§ pattern 偵測` 加同樣三源;fixed.md 仍為 bug 類 pattern 唯一來源(裁定型僅適用「規範被推翻」判準,防止候選浮濫)。
- **影響**:純流程檔修正,不涉 code,無 backward 問題;需同步改 2 檔(07-rule-evolution.md、docs/Prompts/reflect-rules.md);歷史報告依慣例產出的候選(260720 候選 1、260721 候選 3、本報告候選 1)追認合規。
- **driver**:user(流程 owner)

---

## 既有待決議候選 — 本版新增佐證(不重列,僅補證據供決議參考)

| 歷史候選 | 本版佐證 | 說明 |
| --- | --- | --- |
| 260706 候選 1 / 260710 候選 1(拆解白名單 / 拆解紀律) | `task-004-token-endpoints.md:57` | **第 5 例**:拆解白名單未含 service 檔,實作端無法合規遵循 `03-backend`「禁 api 直 import repository」,被迫記錄在案偏離(對外版本層直呼 `ApiClientRepository`)。建議決議時納入子條:「白名單須包含滿足分層規範所需的新檔;實作中發現缺檔應回補白名單並補 task 註記,禁以偏離記錄了事」 |
| 260709 候選 2 / 260710 候選 2(共用測試 DB 使用約定) | scan `260730160810 § ⚪`(`test_semantic_autofill.py:29-35`) | module-level `os.environ` 硬覆寫 pattern **第 5 版佐證**,持續擴散,收斂成本遞增 — 再次建議本輪一併決議 |
| 260721 候選 1(並行與背景狀態安全) | scan `AD-135`(併發 rotate 雙 active)、`AD-136`(未驗證輸入當共享 key)、`AD-146`(drift ADD COLUMN race,**AD-132 同型再現**) | 候選規則第 (1) 條(跨 process 併發共用流程必互斥或可容忍設計)若已生效,AD-135/146 於實作期即會被攔 — 候選價值再獲三條實證,建議優先採納 |
| 260720 候選 1(RDS 時間型別 → `06-timezone`) | scan `260730160810 § 6`(v1.5.2 drift 新欄沿用 naive UTC+8 通則) | 通則持續被日常遵循,規範檔仍未補 — 維持「儘速採納收掉」建議 |

## 不成案說明(v1.6.0 記錄在案候選之逐項處置)

task-007 列的 4 項候選,本輪逐項評估:

1. **對外版本層直呼 repository** → 併入歷史「拆解白名單」候選佐證(見上表),不另立案;task-004 附帶建議「對外端點增多時收攏 `api_client_router` 專屬 service」列入觀察名單 4。
2. **retire 三層路徑 vs 兩層建議**(task-005)→ 不成案:API 路徑深度屬規格定案的設計偏好,無對應規範、無跨版本 pattern、無實害(scan 已確認行為正確)。
3. **`ClientEnvelopeRoute` 位置應挪 `common/`**(task-007)→ 不成案:單檔擺放位置屬重構建議,依「不擴張範圍(重構建議不列)」自我約束排除;對外端點增多時隨觀察名單 4 一起處理。
4. **PATCH `description` 無法清空** → 不成案:`exclude_unset` 部分更新語意為 FastAPI 慣例行為,單一場景、無規則可修;若未來多個資源重演「nullable 欄位無法清空」再議。

## 觀察名單(未達 pattern 門檻,下次 reflect 重評)

1. **對外 API 契約正確性**(新):scan `AD-134`(Retry-After 遵守者永久 429)、`AD-138`(OpenAPI 殘留雙鑰語意)、`AD-133`(前端交付已失效憑證)— 同版三條「對外契約 / 文件與實際行為漂移」。v1.6.0 為對外 API 首發,單版本暫不成案;下一版新增資料端點時若再現,建議落 `03-backend` 新段〈對外契約〉(機器可讀欄位須有「遵守契約必成功」測試、OpenAPI 描述隨裁定同步、憑證展示端 UI 換代即失效)。
2. **02-frontend 字級地板 vs user 表格按鈕裁定**(新):`AD-130`(v1.5.1,依地板修正)→ `AD-149`(v1.6.0,user 裁定按鈕縮小與地板衝突)— 方向相反的兩例,尚不構成穩定 pattern;**先決條件是 user 對 AD-149 的裁定**(收編 → 修規範開「表格內操作按鈕 / 輔助標籤」例外;要求合規 → 修碼,觀察名單撤銷)。
3. **靜態映射查動態實體的漂移防禦**(260721 觀察 1 保留):v1.5.2 已補「新增欄位」方向(drift ADD COLUMN + autofill);「欄位改名 / 消失 → 殘欄與殭屍映射」方向仍無防禦(scan 已巡視節記錄為已知後果)— 維持觀察。
4. **對外層服務收攏**(新,自不成案 1/3 轉入):`api_client_router` 目前一支唯讀查詢直呼 repo + 封套 Route 置於 versions 層,對外端點僅 2 支時可接受;端點增多(Phase 3A 資料端點)時升格評估。
5. **無法佔位的機密格式之 dev 預設值**(新):scan `R-ENV-001 邊界案`(Fernet key 無法用 changeme 佔位,example 帶真值 vs 留空破壞開箱即跑)— 單例;若 user 裁定保留開箱即跑,建議 `00-overview` 機密段補「dev 共用預設金鑰白名單」機制再收。

## 已巡視之 pattern 判準(證明跑過)

- **同規則 ≥3 次違反**:v1.5.2/v1.6.0 零 fixed.md 條目,無規範參照可分組;scan AD-133~150 分屬對外契約 / 併發 / 範圍圈定 / env 完整性四族,單版本內無同一規則 3 犯 → 無強化候選。
- **同類根因跨 ≥2 版本**:內省範圍圈定(v1.5.1 §1 + v1.5.2 AD-145)→ 候選 2;並行家族(AD-132→AD-146 同型)→ 併入 260721 候選 1 佐證,不重列。
- **規範矛盾 / 被推翻**:task-009 裁定推翻 03-passwords-and-pii 適用範圍 → 候選 1;reflect 素材定義與三輪實務脫節 → 候選 3。task-010 推翻的是 propose In Scope(雙鑰並存),propose 屬版本文件可隨裁定調整,非規範推翻,不成案。
- **規則 ≥6 個月未違反(棄用)**:專案 2026-06 啟動未滿 6 個月,不適用,跳過。
- **已決議候選重列檢查**:歷史 19 條候選全數仍為待決議(無 ✅/❌),依規不重列、僅補佐證;無 🕐 暫緩條目需重評。

---

> 本次:候選 3(修正 2 / 新增 1 / 強化 0 / 棄用 0)+ 既有候選佐證 4 組 + 不成案說明 4 條 + 觀察名單 5 條。等 user 決議:✅ 採納開 task(C 段)/ ❌ 拒絕記原因 / 🕐 暫緩下次重評。**提醒:候選累計已達 22 條跨 6 份報告,其中「並行與背景狀態安全」本版再添三條實證、「拆解白名單」「共用測試 DB」均至第 5 例 — 積壓越久同錯越重複,強烈建議 v1.6.0 收口後安排一次集中決議。**
